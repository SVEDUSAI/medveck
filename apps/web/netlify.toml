[build]
  publish = "dist"

# Proxy all /api/* requests to the Render-hosted backend API
[[redirects]]
  from = "/api/*"
  to = "https://medvek-api.onrender.com/api/:splat"
  status = 200
  force = true

# SPA fallback — must be last
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
